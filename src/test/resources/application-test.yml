mq:
  worker:
    queue-name: worker-test
    concurrency: 1-1

spring:
  artemis:
    user: artemis
    password: artemis

fib:
  calculation:
    is-cache-active: false

logging:
  level:
    org.gol: trace