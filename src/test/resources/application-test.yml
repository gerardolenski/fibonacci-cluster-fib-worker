mq:
  worker:
    job-queue-name: job-test
    job-result-queue-name: job-result-test
    concurrency: 1-1

spring:
  artemis:
    user: artemis
    password: artemis

fib:
  calculation:
    is-cache-active: false

logging:
  level:
    org.gol: trace